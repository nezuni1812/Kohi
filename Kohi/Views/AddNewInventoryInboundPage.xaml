<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Kohi.Views.AddNewInventoryInboundPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kohi.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nhập lô hàng" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" HorizontalAlignment="Left" Width="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Margin="0,0,0,5">
                <Run Text="Tên NVL"/>
                <Run Text="*" Foreground="Red"/>
                </TextBlock>
                <ComboBox ItemsSource="{Binding IngredientViewModel.Ingredients}" DisplayMemberPath="Name" PlaceholderText="-- Chọn nguyên vật liệu --"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <TextBlock Text="Đơn vị" Margin="0,0,0,5"/>
                <TextBox PlaceholderText="Đơn vị" IsReadOnly="True"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <TextBlock Text="Số lượng nhập" Margin="0,0,0,5"/>
                <NumberBox Minimum="0" SpinButtonPlacementMode="Inline"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,10">
                <TextBlock Text="Ngày nhập" Margin="0,0,0,5"/>
                <DatePicker/>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,0,0,10">
                <TextBlock Text="Ngày hết hạn" Margin="0,0,0,5"/>
                <DatePicker/>
            </StackPanel>

            <StackPanel Grid.Row="5" Margin="0,0,0,10">
                <TextBlock Text="Nhà cung cấp" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding SupplierViewModel.Suppliers}" DisplayMemberPath="Name" PlaceholderText="-- Chọn nhà cung cấp --"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Width="500" HorizontalAlignment="Left">
            <StackPanel Orientation="Vertical" Width="300" HorizontalAlignment="Left">
                <TextBlock x:Name="outtext" HorizontalAlignment="Left" Text="Tổng giá nhập" Width="Auto" Margin="0,0,0,10"/>
                <NumberBox Minimum="0" SpinButtonPlacementMode="Inline"/>
            </StackPanel>
        </StackPanel>

        <Button x:Name="saveButton" Grid.Row="3" Click="saveButton_click" Style="{StaticResource PrimaryButtonStyle}">
            <StackPanel Orientation="Horizontal">
                <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                <TextBlock x:Name="addButtonTextBlock" Text="Lưu lô hàng" Margin="5,0,0,0"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>
