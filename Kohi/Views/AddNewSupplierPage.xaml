<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Kohi.Views.AddNewSupplierPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kohi.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Thêm nhà cung cấp mới" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" ColumnSpacing="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*"/>
                <ColumnDefinition Width="0.6*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Thông tin nhà cung cấp" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,10">
                <TextBlock Margin="0,0,0,5">
            <Run Text="Họ và tên"/>
            <Run Text="*" Foreground="Red"/>
                </TextBlock>
                <TextBox x:Name="customerName" PlaceholderText="Nhập họ và tên"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,10">
                <TextBlock Margin="0,0,0,5">
            <Run Text="Email"/>
            <!--Run Text="*" Foreground="Red"/-->
                </TextBlock>
                <TextBox x:Name="customerEmail" PlaceholderText="Nhập email"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,10">
                <TextBlock Margin="0,0,0,5">
            <Run Text="Số điện thoại"/>
            <Run Text="*" Foreground="Red"/>
                </TextBlock>
                <TextBox x:Name="customerPhone" PlaceholderText="Nhập số điện thoại"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,10">
                <TextBlock Margin="0,0,0,5">
            <Run Text="Địa chỉ"/>
                </TextBlock>
                <TextBox x:Name="customerAddress" PlaceholderText="Nhập địa chỉ"/>
            </StackPanel>

            <Button x:Name="saveButton" Grid.Row="5" Grid.Column="0" Click="saveButton_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,10,0,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                    <TextBlock Text="Lưu nhà cung cấp" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
