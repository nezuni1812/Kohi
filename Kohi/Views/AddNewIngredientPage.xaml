<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Kohi.Views.AddNewIngredientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kohi.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Thêm mới nguyên vật liệu" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" ColumnSpacing="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.6*"/>
                <ColumnDefinition Width="0.4*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Thông tin chung" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,0,10"/>

            <Grid Grid.Row="1" Grid.Column="0" ColumnSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Margin="0,0,0,5">
                <Run Text="Tên sản phẩm"/>
                <Run Text="*" Foreground="Red"/>
                    </TextBlock>
                    <TextBox PlaceholderText="Nhập tên nguyên vật liệu"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
                    <TextBlock Margin="0,0,0,5">
                <Run Text="Đơn vị"/>
                <Run Text="*" Foreground="Red"/>
                    </TextBlock>
                    <TextBox PlaceholderText="Nhập đơn vị (Kg, Lít, Cái,...)"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Margin="0,0,0,5">
                        <Run Text="Giá nhập trên mỗi đơn vị"/>
                        <Run Text="*" Foreground="Red"/>
                    </TextBlock>
                    <NumberBox 
                        Minimum="0" 
                        SpinButtonPlacementMode="Inline" 
                        Width="200" HorizontalAlignment="Left"/>
                </StackPanel>
                
            </Grid>

            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,10">
                <TextBlock Text="Mô tả" Margin="0,0,0,5"/>
                <TextBox
                     AcceptsReturn="True" 
                     Height="50" 
                     TextWrapping="Wrap" 
                     PlaceholderText="Nhập mô tả nguyên vật liệu"/>
            </StackPanel>

            <Button x:Name="saveButton" Grid.Row="4" Grid.Column="0" Click="saveButton_click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,10,0,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                    <TextBlock x:Name="addButtonTextBlock" Text="Lưu nguyên vật liệu" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>
            
        </Grid>
    </Grid>
</Page>